#include<cstdio>
#include<cstring>

const int maxn=100+10;
const int INF=1<<30;

char s1[maxn],s2[maxn];
int d[maxn][maxn;

int main()
{
    int len;
    while(scanf("%s",s+1)!=EOF)
    {
        scanf("%s",t+1);
        len=strlen(s+1);
    }
    return 0;
}
